Transposition: Add a button to the Joplin toolbar that sends a message to the content script to shift all detected chords by a certain number of semitones.

Autocompletion: automatically complete chords (based on chords already in file/block), chordpro directives, and section labels.

Provide styling and header options for all section types (verse, chorus, tabs, bridge, etc). e.g. hide/show header, header colour, section font size, section font colour, section border enabled or not, section border colour etc. 